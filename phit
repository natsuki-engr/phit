#!/usr/bin/env php
<?php

require_once('./command/Init.php');
require_once('./command/Add.php');

$commands = [
	Init::class,
	Add::class,
];

$subCommand = $argv[1] ?? '';
echo '$argv: '; print_r($argv); echo PHP_EOL;
array_pop($argv);
$options = Options::format($argv);

// foreach($commands as $command) {
// 	if($command::$command === $subCommand) {
// 		$class = new $command();
// 		$class->execute(array_slice($argv, 2, $argc - 2));
// 	}
// }

if(empty($class)) {
	echo "Invalid command: $subCommand\n";
	exit(1);
}
